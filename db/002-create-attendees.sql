CREATE TABLE attendees (id INTEGER AUTO_INCREMENT, name VARCHAR(256) NOT NULL, channel_id INT NOT NULL, sid VARCHAR(256) NOT NULL, PRIMARY KEY(id), INDEX(channel_id), INDEX(sid), FOREIGN KEY (channel_id) REFERENCES channels(id) ON DELETE CASCADE);
